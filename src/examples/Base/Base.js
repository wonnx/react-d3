import React, {useState, useEffect, useRef} from "react";
import {select, selectAll} from "d3"

const Base = () => {
    return (
        <React.Fragment>

        </React.Fragment>
    )
}

export default Base;